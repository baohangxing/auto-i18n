import { CLI_CONFIG_NAME, CONFIG_FILE_NAME, PROJECT_NAME } from '@h1mple/auto-i18n-core'

export const LOG_PREFIX = PROJECT_NAME

export const KEY_SYMBOL_IN_XLSX = 'key'

export { PROJECT_NAME, CLI_CONFIG_NAME, CONFIG_FILE_NAME }
